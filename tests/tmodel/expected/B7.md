# B7 — trigger on an interface item fires, link should color
Expected: getRelations()/getDevices() don't currently resolve trigger severity onto links or
nodes at all (unlike the G-model's describeSeverity() on nodes, which IS wired in for
compatibility, §-node severity works; link-level severity coloring was never built in this
pass). Expect device/node severity to work (already shown live: Router1 etc. carry severity
fields), but link/port-level severity-based coloring to be absent -- record as a real gap in
THIS implementation (not the model itself, since §2.3's "problem styling — derived, not
stored" from the G-spec is exactly the same live-join pattern that COULD be added here), verdict
BUG (missing feature relative to the spec's own §6 promise: "trigger severity on links ...
live-joined exactly as in the G-model") rather than MODEL_LIMIT.
