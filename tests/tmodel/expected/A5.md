# A5 — two hosts share the same inventory MAC (Clone host simulation)
Expected: the assembler's mac-binding candidates collect BOTH hosts; §5.3's "several hosts, one
cluster" rule picks the lowest hostid to draw and flags conflict=true — NOT a silent
first-wins with no trace. Verify getDevices()'s conflict field and diagnostics()'s conflicts
list both surface it.
