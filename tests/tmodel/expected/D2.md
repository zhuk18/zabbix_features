# D2 — export/import; Clone host
Expected: exporting a reporter Host (with its topo.* tags and inherited template) and
re-importing onto a clean instance should bring the tags back verbatim (they're plain Host
tags, nothing exotic) and the template attachment recreates topo.self/topo.nbr.discovery/
topo.nbr[] on the next push, per constraint 1's "no state outliving Zabbix objects" design.
Clone Host duplicating topo.id is the SAME mechanism as A5's conflict (a second host now
carries the identical topo.id value) -- expect it to surface exactly the same way A5 does, not
a special case.
