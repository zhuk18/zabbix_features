# B3 — one side stops reporting
Expected: already run live in t-model-findings.md's E-B3(a) with real timestamps
(last_seen_src frozen, last_seen_dst advancing, aggregate last_seen masking the frozen side).
This entry re-confirms the SAME mechanism using the harness's isolated test hosts rather than
the shared lab hosts, and additionally checks E-B3(b) (one side's neighbor disappearing while
that side KEEPS pushing) end-to-end against real topo.nbr items, which t-model-findings.md
flagged as not separately re-verified in the final pass.
