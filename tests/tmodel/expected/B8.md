# B8 — cable moved from port 24 to 25
Expected: old link (port 24) has no more confirming observation on either side once the blob
changes -- it should stop being drawn (both sides' item value becomes "present":false, or the
item simply stops existing if if_index 24's neighbor entry is gone from the blob entirely).
New link (port 25) appears fresh. "What was on port 24 last week" -- answerable via the OLD
port-24 item's own Zabbix history for as long as: (a) lifetime_type=NEVER means the item is
never deleted just for going lost, and (b) history retention (this template's item history
setting) hasn't expired it. Test and report the actual retrieval path (item.itemid ->
history.get), not just claim it works.
