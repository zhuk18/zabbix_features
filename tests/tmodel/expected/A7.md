# A7 — stack (one host, two topo.id) vs BMC+OS (two hosts, one chassis_id)
Expected: stack case (one host manually tagged with two topo.id values, e.g. two chassis
clusters) -- ✅, getDevices() should show one node with node.devices having 2 entries, no
conflict flagged (this is the intended multi-device case, not an error). BMC+OS case (two
DIFFERENT hosts both getting topo.id/mac-matched to the SAME cluster) -- this is
STRUCTURALLY IDENTICAL, at the assembler's data-shape level, to A5's error case (several
hosts, one cluster) -- the assembler has no way to distinguish "this is a legitimate BMC+OS
pair" from "this is an accidental duplicate/clone" without a human telling it which. Expect
this contradiction to be real and reproducible: record as MODEL_LIMIT.
