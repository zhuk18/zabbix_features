# A6 — proxy as a topology node
Expected 🔴 direct (confirmed by Q3: no proxy tags in 8.0). Test the documented workaround:
tag topo.id on the Host running the proxy process (if monitored as its own Host) — expect this
to work exactly like any other host binding (nothing proxy-specific about it once you're
tagging a Host), but note it represents the proxy's HOST machine, not the Zabbix Proxy object
itself, which stays invisible to the graph either way.
