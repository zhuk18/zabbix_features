# Q2 — which fields does selectItemDiscovery return?
Expected (from t-model-findings.md's V1, already run): parent_itemid, key_, status,
ts_delete, ts_disable, disable_source. NOT lastcheck (exists in the DB, rejected by the API
as an invalid selectItemDiscovery field). Re-confirmed here with a fresh API call.
