# D7 — scale: 200/500/1000 reporters x 48 ports, 30% LLDP-MED phones, 3x each, median reported
Expected: NOT RUN this pass -- flagged upfront, not silently skipped. Building a synthetic
multi-reporter blob generator at this scale, actually creating that many Zabbix hosts+template
attachments, and waiting out real LLD processing for each size x3 is a substantial, multi-hour
undertaking on its own; per the doc's own "stop when a step exceeds 5 minutes / UI unusable"
framing this also risks materially degrading the shared dev instance other work depends on.
Recorded as BLOCKED-by-scope-for-this-pass, with a note on what a follow-up run would need.
