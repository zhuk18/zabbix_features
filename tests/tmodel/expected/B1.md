# B1 — port-to-port links; ports without neighbors visible via standard interface items
Expected: getPorts() lists every port from the blob (connected and disconnected), each
connected one showing the interface tag value; drill-down data (mac/status/speed) present.
The `interface` tag on the neighbor item should equal the SAME local port name that shows up
in getPorts() for that if_index, letting a real standard-network-template trigger inherit it.
