# B6 — LAG
Expected BLOCKED at the collector level: this implementation's push.py/blob schema has no
ieee8023adTable walk and no lag_members field (G-spec's own tracked gap, carried over
unchanged per constraint 3). At the model level only: two LAG member ports would show up as
two entirely separate, unrelated physical_link rows (no aggregation concept exists in either
the blob or the assembler) -- readable but not aggregated. Record as MODEL_LIMIT for the
collector gap, not a T-model-specific defect (constraint 3 explicitly keeps the transport/
collector identical to the G-model, gaps and all).
