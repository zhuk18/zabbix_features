# A4 — neighbor becomes a reporter, both orders
Expected ✅ (use-case doc's claim): whichever order the two reporters' blobs land in, the
canonical id computed for the shared neighbor is identical (pure function of chassis_id/
sysname+scope), so clustering collapses them to one node regardless of order — no merge step,
no duplication window. Test: push order (existing-reporter-sees-future-reporter-as-neighbor
first, then the future reporter's own self-push) vs the reverse order; graph should be
identical either way.
