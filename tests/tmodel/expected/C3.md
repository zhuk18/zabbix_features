# C3 — hostile LLDP strings + id > 255 chars
Expected: nbr_discovery.js's truncate255() caps {#LOCPORT}/{#PEERNAME} at 255 chars;
canonicalPeerId() falls back to a hashed "h:<hash>" id when the raw id would exceed 255 chars
(implemented, per spec §3.2's macro table). Two DIFFERENT long ids colliding onto the same
hash would be a real (if extremely unlikely with a 64-bit hash) silent-collision risk --
not proven impossible, just improbable; record as SPEC_GAP if no collision-detection exists
(it doesn't, currently). XSS payloads (<script>, "><img onerror>) in sysname: this
implementation has no browser-rendered UI review possible in this environment (no headless
browser here) -- the PHP-side data path is checked instead: confirm the raw string travels
through getDevices()/getPorts() UNESCAPED (escaping is a render-time frontend job, per §8,
using the same shared sanitization helper as the G-model, per constraint 5) -- i.e. confirm
this implementation does NOT double-escape or mangle the string server-side, and separately
confirm (by code reading, not a live browser) that escape() is called at every render point in
monitoring.topology.view.js.php. Mark the actual in-browser verification BLOCKED (no
Playwright/browser available in this environment).
