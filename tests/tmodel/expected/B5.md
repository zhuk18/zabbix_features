# B5 — delete discovered item + suppress
Expected: item.delete (Q1/V3) removes it; if the LLD row is still being emitted, next push
recreates it (already shown live). unlinkPorts()'s dismiss path (this implementation's name
for the use-case doc's topo.link.suppress) should hide the discovered observation going
forward — test that after dismissing, the link stops being drawn even though the underlying
item/tags are technically still there, and that maintenanceCleanup() can remove a dismiss tag
whose target is gone.
