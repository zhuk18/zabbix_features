# C2 — reader called twice on identical state; host/item order shuffled
Expected: byte-identical graph (same node ids, same link set) on repeated calls -- the
assembler has no random iteration-order dependency that SHOULD leak into output (ids are
computed from content, node/link arrays are built deterministically per input order but their
CONTENTS, once sorted for comparison, must match). Already partially shown in
t-model-findings.md (3 consecutive reads identical); this re-verifies plus explicitly checks
sorted-id equality rather than raw array equality (which WOULD be order-sensitive and isn't
the actual claim being tested).
