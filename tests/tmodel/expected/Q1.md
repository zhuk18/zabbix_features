# Q1 — can a single discovered item be deleted (UI and API)?
Expected (from t-model-findings.md's V3, already run): item.delete on one discovered
topo.nbr item succeeds via API; if its LLD row is still being emitted, the next push
recreates it. UI path not tested (no browser in this environment) — API path re-confirmed here.
