# D3 — bulk queries ("all stale links", "all unmonitored devices")
Expected: both are a single pass over getRelations()/getDevices()'s already-assembled output
(filter stale==true / bound==false) -- no extra API calls beyond the fixed 4 that assemble()
always does (§5.1). Correctness should hold at this lab's scale trivially; the real question
(time at scale) is D7's, not this one's -- D3 here just confirms the filtering logic itself is
correct, using the current lab's known stale/unbound set as ground truth.
